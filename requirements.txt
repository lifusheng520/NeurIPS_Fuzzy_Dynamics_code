# Supported experiment environment: Python 3.10-3.12 on Linux x86_64.
# Install a PyTorch wheel compatible with the cluster CUDA driver when the
# default resolver cannot select the correct build.

# Core fuzzy-dynamics pipeline and local LLM extraction
# torch>=2.4,<3
transformers>=4.51,<5
accelerate>=0.27,<2
numpy>=1.26,<3
pandas>=2.0,<3
datasets>=2.17,<5
tqdm>=4.66,<5
requests>=2.31,<3
matplotlib>=3.8,<4
PyYAML>=6,<7
sentencepiece>=0.2,<1
safetensors>=0.4,<1
packaging>=23,<27

# Layer-wise decoding and imported latent multi-hop baselines
tuned-lens==0.2.0
unidecode>=1.3,<2
fancy-einsum==0.0.3
baukit @ git+https://github.com/davidbau/baukit.git@9d51abd51ebf29769aecc38c4cbef459b731a36e

# vLLM is intentionally not installed here. Its binary must match the server's
# CUDA/PyTorch stack. The latent multi-hop scripts support --backend hf without
# it and raise an actionable error only when --backend vllm is selected.
